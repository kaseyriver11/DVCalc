# DVC Companion

## Overview
A web app for Disney Vacation Club owners and renters — started as a points calendar (nightly cost in points and estimated cash equivalent for any resort/room/date) and has grown into a fuller companion covering the rest of DVC ownership: trip planning and saved itineraries, contract tracking and points ledgers, a buying-guide contract-value comparison, and a Trophy Case of ownership/planning achievements. Renamed from "DVCalc" 2026-09-18 to reflect that broader scope — see the Mobile Responsiveness Guardrails section below for one example of a convention that now applies app-wide, not just to the original calendar page.

## Status
Live at dvccompanion.com (GitHub Pages, deployed on push to `main`), with Supabase (`dvcalc_start`) for accounts, owner data, and Edge Functions. Core calculator, owner tools (contracts, points ledger, trips, itineraries, Trophy Room), and the nightly data pipeline are all shipped. Active Member billing is built against Stripe **test mode** only -- `MEMBERSHIP_GATE_ENABLED` ships `false` until go-live (`docs/subscriptions_plan.md` Phase 6). Open work lives in `docs/*_todo.md` and the plan docs; there are no TODO comments in code.

## Tech Stack
- **Frontend:** HTML/CSS/JavaScript (vanilla, no framework, no build step). Shared logic is plain `<script>` files exposing `window.DVC*` globals, most also `require()`-able by Node for tests.
- **Hosting:** Static files on GitHub Pages (`CNAME` -> dvccompanion.com); a PWA via `manifest.json` + `service-worker.js`
- **Backend:** Supabase -- Postgres with RLS (`db/schema.sql`, `db/migrations/`), auth, Edge Functions (`supabase/functions/`)
- **Data:** JSON embedded in JS under `data/` (points charts extracted from DVC PDFs, plus cash, dues, availability, events)
- **Tests:** `node --test tests/*.test.js` -- plain `node:test`, no dependencies

## File Structure
```
Pages
├── home.html / home.js         # Home dashboard (health banner, balances, deadlines)
├── index.html / app.js         # Points calendar -- the original tool; Smart Draw, review mode, split stays
├── compare.html                # Cross-resort date comparison (self-contained)
├── changes.html                # Year-over-year point changes (self-contained)
├── suggest.html                # Suggest a Stay -- points budget -> stays (self-contained)
├── contractvalue.html          # Contract Value -- which resort to buy (self-contained)
├── account.html                # My Contracts -- contracts, points ledger, point moves
├── trips.html                  # Membership Value -- logged trips, House Money payback
├── itineraries.html            # Saved itineraries
├── itinerarycompare.html       # Compare up to 3 itineraries
├── badges.html / badges_info.html  # Trophy Room and badge reference
├── privacy.html / terms.html   # Legal
└── styles.css / tokens.css     # Calendar styles / shared design tokens and components
Shared scripts (window.DVC* globals)
├── auth.js                     # Supabase auth + every owner-data read/write; Active Member gate
├── nav.js                      # Mobile nav toggle + service worker registration (PWA install)
├── dvc-ui.js                   # Custom select component
├── dvc-dates.js                # Use-year, banking-deadline, and booking-window date math
├── dvc-ledger.js               # Points-ledger rules: borrow cap, Holding, cancel outcomes
├── dvc-balances.js             # Balance entry ("what's left") helpers
├── dvc-point-moves.js / account-point-moves.js  # Bank/borrow move previews / My Contracts sheets
├── dvc-point-attention.js      # Per-contract deadline/expiry events (Home + My Contracts)
├── dvc-leftover-points.js      # Short-notice stay ideas for expiring points
├── dvc-plan-funding.js         # Allocate contract balances across a stay's nights
├── dvc-trip-funding.js / dvc-trip-deduct.js  # Trip point attribution / ledger deductions
├── dvc-owner-value.js          # Ownership value / House Money model
├── dvc-compare-handoff.js      # URL handoff that opens the calendar on a given stay
├── dvc-badges.js / dvc-track.js  # Trophy Room logic / event-count tracking
└── service-worker.js           # PWA service worker
data/                           # data.js (2026-27 charts, cash, dues), data_historical.js (2016-2027, generated),
                                # availability, live cash prices, events, construction, investment, images, shorthand
db/                             # schema.sql, migrations/ (run by hand in the Supabase SQL editor), check_migrations.sql
supabase/functions/             # Edge Functions: banking reminders, unsubscribe, Stripe checkout/portal/webhook
scripts/                        # PDF fetch/download/extract, live cash-rate build, nightly watchdog
.github/workflows/              # nightly-digest.yml -- nightly pipeline + morning digest
docs/                           # Plans, audits, and *_todo.md lists of open work
tests/                          # node --test suites
pdfs/{resort}_archive/          # Source PDFs for the extract scripts -- NOT tracked (gitignored); regenerate with the download scripts
```

## Key Features
1. Searchable resort selector dropdown (alphabetical)
2. Room type selector (dynamic per resort)
3. Year selector (2016-2027, historical data for all resorts)
4. Monthly calendar grid with points-per-night displayed
5. Color coding by travel periods (season)
6. Check-in / check-out date selection
7. Side-by-side layout: calendar left, cost breakdown right
8. DVC rental rate slider ($15-$25/point, default $20)
9. Owner mode (calculates cost based on annual dues per point)
10. Summary: total points, rental value, Disney cash rate, savings %
11. Nightly breakdown (expanded by default)
12. Custom cash rate input for non-WDW resorts
13. Responsive design
14. Defaults to current month

## Resorts
All 17 DVC resorts are selectable for 2026 and 2027, and `data.js` now defines all 17 (Animal Kingdom Villas was added 2026-08-30 — see below). Cash rates now cover all 17: the 12 WDW resorts from MouseSavers, Disneyland Hotel from the live pricing pipeline, and Aulani/Grand Californian/Vero Beach/Hilton Head from rough manual estimates (`estimatedCashRates: true` in `data.js` — see `docs/data_reproducibility.md` "Estimated Cash Rates" for methodology and confidence caveats). Every estimated-rate resort gets a "(!)" hover badge (`estimateBadgeHTML()` in `app.js`/`compare.html`) next to its name and on the Cost Comparison tile, explaining the price isn't an observed rate.

**Animal Kingdom Villas** points/cash rates live directly in `data.js` for 2026 only (Jambo House + Kidani Village MouseSavers pages, averaged where both buildings share a view). 2027 has no `data.js` entry — it still comes from the generated `data_historical.js` for points, and falls back to 2026's cash rates via `getCashRateWithFallback()` (same prior-year fallback other resorts use), since MouseSavers' 2027 AKV pages don't yet publish December rates.

All 17 resorts have historical points data back to 2016 (or their opening year) in `data_historical.js`, auto-extracted from DVC Field Guide PDFs. Two gaps exist: BWV 2018 and PVB 2016 are image-based PDFs that can't be text-extracted (see `docs/data_reproducibility.md`).

---

## Data Sources

For detailed data documentation, reproducibility audit, and update instructions, see [`docs/data_reproducibility.md`](docs/data_reproducibility.md).

| Data | Source | How to Update |
|------|--------|---------------|
| Points charts | Official DVC PDFs from Disney's CDN | Run `scripts/fetch_pdf_urls.sh` to get URLs, extract values from PDFs, add to `data.js` |
| Historical points | DVC Field Guide point-archive PDFs | Run `scripts/download_historical_pdfs.sh`, then `scripts/extract_all_historical.py pdfs/` |
| Cash rack rates | MouseSavers.com (WDW only) | Scrape rate tables, map to DVC periods, add as `cashRates` in `wdwPeriods()` |
| Annual dues | dvcresalemarket.com | Update `DUES_PER_POINT` in `data.js` |
| Rental rates | Market observation | Slider is user-configurable ($15-$25/pt) |
| Hotel construction/refurbishment | Disney Food Blog's DFB Disney World Calendar PDF | Re-read the latest PDF, update `data/resort_construction.js` |
| Special events (festivals, hard-ticket parties, runDisney) | Disney Food Blog's DFB Disney World Calendar PDF | Re-read the latest PDF, update `data/disney_events.js` |

---

## Architecture Notes

### data.js
- `DUES_PER_POINT` — annual dues by resort ID
- `wdwPeriods(rates, cashRates)` / `wdwPeriods2027()` — helpers that create travel period objects for WDW resorts (shared season dates)
- `RESORTS[]` — array of resort objects with `id`, `name`, `year`, `roomTypes[]`, `travelPeriods[]`
- Lookup functions: `getTravelPeriod()`, `getPointsForDate()`, `getCashRateForDate()`

### data_historical.js
- `buildPeriods(periodDefs, rates)` — generic period builder for years with varying structures
- `WDW_YYYY` constants — period definitions (name, color, dateRanges) per year
- Pushes historical resort entries onto `RESORTS[]` (loaded after data.js)
- Covers all 17 resorts, 182 resort-year entries (2016-2027, varying by resort opening date)
- Handles 5-period (2016-2020: Adventure, Choice, Dream, Magic, Premier), 6-period, and 7-period (2021+) structures
- Also handles Aulani flat-rate (SUN-SAT), non-WDW period structures, and variable column counts per resort
- Generated by `scripts/extract_all_historical.py` from PDF source files
- 2026/2027 entries overlap with `data.js`, so the file ends with a `dedupeResorts()` IIFE that keeps the first entry per `(id, year)`. Since `data.js` loads first, its entries win (they include cash rates) and the resort dropdowns list each resort once. The footer is emitted by `extract_all_historical.py`, so regenerating preserves it.

### app.js
- `state` object tracks selections (resort, room type, year, month, check-in/out, rental rate, owner settings)
- Calendar renders a 7-column grid with the point cost as each cell's centered hero number and the cash rate as a smaller line right under it; the date number, crowd score, and 🎉 event icon are each pinned to their own corner of the cell (top-left/top-right/bottom-right) so they can't collide. The 2026-09-19 declutter pass had dropped the cash rate entirely and swapped the 🎉 for a plain corner dot, but both were reverted the same day on user feedback that it read as "too boring" — the SUN-THU/FRI-SAT sub-label stays dropped, though, since the day-of-week header already conveys that
- Summary panel shows totals with conditional sections based on available data
- Year-aware: `getResort()` filters by both `resortId` and `year`
- Calendar cells show a 🎉 badge (bottom-right corner) only for genuinely date-specific entries from `getEventsForDate()` (`data/disney_events.js` categories `hardTicketEvent`/`runDisney`/`singleDay` — not `festival`/`seasonal`, see `BROAD_EVENT_CATEGORIES`), with a hover tooltip listing them — credited to Disney Food Blog. This scoping (kept from the 2026-09-19 declutter pass even after the emoji itself came back) is what keeps a multi-week festival from painting the badge across all 30+ days it runs; that broader window is surfaced once instead, in the "Special Events & Festivals" summary card (`buildSpecialEventsHTML()`)
- The summary panel shows a "Resort Alerts" card (`buildResortAlertsHTML()`, sourced from `data/resort_construction.js`) whenever the selected resort has known ongoing/upcoming construction or refurbishment; entries overlapping the selected stay dates are called out with a "During your stay" badge. Alerts render *before* "Your Stay" while browsing (no dates picked yet) as a "consider this while picking a date" prompt, but move to *after* it once dates are selected, so they read as supporting context rather than the top-billed card
- "Booking As" (contract picker) and "Load Trip" (saved-itinerary picker) live in the top control bar (`#booking-as-control`/`#itinerary-load-control` in index.html) as real `initCustomSelect()`-driven custom-selects — same persistent-`<select>`-plus-styled-trigger component Resort/Room Type use, not a plain native `<select>` — so they match the theme and the trigger's fixed width (narrow for Load Trip) keeps long names from blowing out the control bar; the dropdown's option list still shows full names (wrapping, not truncating). `renderBookingAsControl()`/`renderItineraryLoadControl()` repopulate `contractSelect`/`itineraryLoadSelect`'s `<option>`s and call `._customSelectRender()` — the same one-time-`initCustomSelect()`-then-repopulate pattern as `populateRoomTypes()`/`roomSelect`, not a full innerHTML rebuild. The old "Points Year" dropdown was removed entirely — crossing a year boundary is only reachable by paging months now (`prevBtn`/`nextBtn` already handled that boundary; `state.year` is otherwise unchanged). Only the per-stay eligibility/points feedback (`buildContractEligibilityHTML()`) lives in the "Your Stay" card, not the top bar
- **Multi-Contract Split** (`buildContractEligibilityHTML()`, same function as above — living there means both `renderSummary()` and `renderTripRail()`/review mode get it for free): with 2+ active contracts, real dates picked, and a single-resort stay (never during an actual split STAY across resorts — `multiContractSplitMode` is named deliberately unlike `isSplitMode()` to keep those two "split" concepts apart in code and copy), a "Split points across contracts instead →" link toggles from the normal single-contract Smart Draw view into `buildMultiContractSplitHTML()`: one `<input type="range">` slider per contract that can book the resort at all (`eligibleSplitContracts()` — a contract that's hard-blocked from this resort, e.g. resale-restricted to a different home-only resort, is left out entirely; that's a different case from a 7-month contract whose window just hasn't opened yet, which still gets a row). Dragging a slider updates that row's own numbers and the shared totals line via direct DOM writes (`updateMultiSplitRowLive()`), not a full re-render, so the slider doesn't lose its drag state — same reasoning as account.html's `updateLedgerRowTotal()`. Defaults to a greedy fill (drain the first eligible contract before touching the next, mirroring `computeSmartDraw()`'s own bucket-priority spirit one level up) rather than starting every slider at zero, since sliders are for overriding a starting point, not building one from nothing. "Apply Split" is disabled until the total allocated exactly equals the stay's point cost (no partial-payment concept for one reservation) and, once enabled, runs `computeSmartDraw()` **per contract** against its own slider amount (so each contract still draws Holding→Banked→Borrowed→Current internally) and writes every result via the same `upsertContractYearPoints()` Smart Draw and the ledger itself use — with a combined Undo snapshot across every touched contract, same 8-second visibility window as Smart Draw's own undo. Item 2 of the task this shipped from ("alert if non-home resort points are allocated before the 7-month window opens") is `contractWindowNotYetOpenWarning()` — shown per-row (regardless of whether that row currently has any points allocated, so the warning is visible before the owner even touches that slider) whenever `getContractWindowMonths()` returns 7 and the stay's check-in date is still beyond `monthsFromTodayCutoff(7)`; the warning states the actual date the window opens (`monthsBeforeCheckIn()`), not just that it's closed
- **11-to-7 Swap Simulator** (also in `buildContractEligibilityHTML()`, same one-function-covers-both-render-paths reasoning as Multi-Contract Split above): only offered when the currently-browsed resort IS the selected contract's home resort (`getContractWindowMonths() === 11`) and real dates are picked -- modeling the common DVC strategy of confirming home resort at 11 months as a safety net, then trying to swap to somewhere else once the 7-month all-resorts window opens. A "Planning to swap at 7 months?" toggle (`swapSimulatorMode`) reveals two `.custom-select` pickers (target resort, then that resort's room types -- `attachSwapSimulatorListeners()` calls `window.DVCUI.initCustomSelect()` on both after every render, from inside `attachStayActionButtonListeners()` so it's covered by every call site that already invokes that) and, once both are picked, shows: the exact 11-month (home) and 7-month (swap) window-opening dates (`monthsBeforeCheckIn()`, the mirror image of `monthsFromTodayCutoff()` -- that one answers "is the window open yet," this one answers "what date does/did it open"); the Point Delta between the two stays' point costs (`computeStayEntry()` on each, reused as-is); and a borrowing-risk read. That risk calc deliberately does NOT reuse `computeSmartDraw()` -- a first pass did, and shipped a real bug (caught the same day, before users saw it) where it understated what a contract could cover, because `computeSmartDraw()` only draws from whatever's already RECORDED as borrowed in the ledger rather than the contract's actual borrowing ceiling. The fix models borrowing as elastic capacity instead: `nonBorrowedAvailable` (remaining+banked+holding) covers what it can, and whatever's left for each of the two stays is compared against `maxBorrowable` (`contract.points_per_year`, same proxy `validateBorrowedPoints()` uses). `extraBorrowNeeded` is the swap-specific delta in borrowing requirement (target's shortfall minus home's own shortfall, so a contract that already needs to borrow for the unswapped home stay isn't double-counted); a danger guardrail fires instead if even the target's own shortfall alone exceeds `maxBorrowable` outright. The warning copy is accurate to DVC's real rule (confirmed via DVC Resale Market/DVC Rental Store, 2026-09-19 research): borrowing is a final transaction, points can't be moved back to next year even if the trip that prompted the borrow falls through -- so an unnecessary borrow for a swap that doesn't pan out isn't recoverable, just left over. (This same research is what caught `dvc-ledger.js`'s `MAX_BORROW_RATIO` bug above.)
- **Two-phase layout ("review mode")**: once a complete single-resort stay is picked, `isReviewMode()` (`!isSplitMode() && getStayDates().length > 0 && !forceExpandCalendar`) collapses the full calendar into a compact "trip rail" card (`renderTripRail()`, into `#trip-rail-view`) — resort/room, dates, a mini per-night points strip laid out as a real 7-column CSS grid with leading blank cells (so it lines up by day-of-week like the full calendar and every night stays equal-width, instead of a flex-wrap approach where a partial last row's items stretch to fill it), points, contract eligibility, then Edit dates/Clear Selection/Add Another Resort/Compare All Resorts/Save Itinerary all stacked together (the shared `buildStayActionButtonsHTML()` + `attachStayActionButtonListeners()` — also used by the normal `#action-buttons` slot in non-review layouts). The summary panel expands (`.summary-side.expanded`) into: Cost Comparison leading, then Stay Insights full-width (its `<h3>` and the "Find Alternatives" button share a `.card-header-row` — title left, button right, instead of the button stacked full-width below the card's content), then Booking Outlook, Special Events & Festivals, and Resort Alerts stacked full-width in that order (the old side-by-side `.insights-columns`/`.insights-col` pairing of Booking Outlook with Crowd Forecast was removed 2026-09-19 along with Crowd Forecast itself — Booking Outlook had no remaining partner card, so it just renders full-width now) — Resort Alerts' own entries still lay out via `.resort-alert-list`'s `auto-fit`/`minmax(240px, 1fr)` grid, so multiple alerts sit two-up once the card's wide enough instead of always stacking single-file. No separate "Your Stay" card in review mode — the trip rail already covers it. `forceExpandCalendar` resets to `false` (re-collapsing) whenever a fresh stay gets completed or cleared; clicking the *current* check-in date again while a complete range is showing clears both dates outright (`handleDateClick()`) rather than re-priming checkIn to the same value, which would otherwise silently turn the next click into a checkout instead of a fresh check-in. Split-stay mode never enters review mode — the full calendar and original action-buttons placement stay unchanged there. `renderLayoutMode()` (tail of `renderCalendar()`/`renderSummary()`) toggles `#full-calendar-view`/`#trip-rail-view` visibility. The Cost Comparison "If booking through Disney" tile folds its own custom-rate input inline (`showCustomRateInput`/`showDisneyTile` in `renderSummary()`) rather than showing a separate input block above the tile grid — same `.cost-tile-rate-input` pattern the rental tile already used. That input is a fallback for resorts with NO published cash rate at all (non-WDW) — it doesn't appear for a resort that has real rate data, even if that number looks surprising; see `data/data.js`'s per-period `cashRates` for the actual stored Sun-Thu/Fri-Sat figures. The trip rail's mini strip also shows 2 dimmed/dashed "context" days just outside the stay on each end (computed fresh each render, so they always sit just past whatever the current edges are) — clicking one extends check-in/check-out to include it, and clicking the first or last *real* night removes it (shrinking that edge inward, or clearing the whole selection if it was a 1-night stay), all via `adjustTripEdge()`. Interior nights aren't clickable — removing one would split the stay into two ranges, which the single check-in/check-out model can't represent
- **Special Events & Festivals** (`buildSpecialEventsHTML()`, 2026-09-19, replacing the old standalone Crowd Forecast card — crowds are already benchmarked in Stay Insights' distribution chart and on the calendar cells' own crowd badge, so a third restatement didn't earn its space) sits directly above Resort Alerts and filters `getEventsForDate()` against the picked stay window: broad `festival`/`seasonal` windows list once as "Active Festivals" with their full running range (e.g. "Aug 27 – Nov 21"), while date-specific `hardTicketEvent`/`runDisney`/`singleDay` entries list under "Date-Specific Events" with the exact stay nights they land on (e.g. "Active on Sep 11, Sep 15"), each tagged with a category badge. Shows an explicit empty state ("No major festivals or ticketed events scheduled during this stay.") rather than disappearing, so its absence from the panel always means "checked, nothing found" rather than looking broken. Not shown for non-WDW resorts (`NON_WDW_RESORT_IDS`) since these are all Walt Disney World park events
- Stay Insights' three distribution bars (`buildDistributionHTML()`) color their "current stay" bar green-to-red (`distGoodBadColor()`, keyed off the same percentile the callout text already shows) for Crowd forecast and Points cost only — both are unambiguous "lower is better," so a calm/cheap stay reads as green at a glance instead of needing the percentile sentence to know if a low number is actually good. Cash price stays neutral purple deliberately: the Value Score right above these bars treats a *high* cash-per-point as the favorable direction (better rental value), so coloring raw cash red-for-expensive would visually contradict that
- The "Find Alternatives" modal's "Also check other resorts" toggle (`state.altCrossResort`) reveals a second "WDW (Orlando) only" checkbox (`state.altWdwOnly`) that restricts cross-resort candidates to `NON_WDW_RESORT_IDS`-excluded resorts, same set and same filtering approach `compare.html`'s own WDW-only toggle uses. Only shown once cross-resort search is on — filtering by region is meaningless when the search is already scoped to a single resort
- Every `tooltip-anchor` (crowd/value/distribution hovers, calendar day cells, trip rail chips, booking outlook dots) gets its `cursor: pointer` and `user-select: none` from the shared base rule in `styles.css`/`compare.html`, not per-element — a bare span/div with text or an emoji directly inside it (a crowd number, a 🎉, a value-score badge) otherwise falls back to the browser's default text-select (I-beam) cursor on the glyph itself even with a pointer-cursor ancestor, since `cursor: auto` resolves per-content rather than by inheritance. Keeping it on the base class is what makes every hover in the app look and feel the same instead of drifting element by element

### changes.html (self-contained)
- Week navigator (prev/next) replaces the old 52-row table
- Multi-combo comparison: "+" button pins resort/room selections as colored series (max 4)
- `getAllCombos()` merges live selector combo with pinned combos, deduplicating
- Chart: SVG with per-year column hover zones → HTML tooltip showing all combos' values
- Y-axis uses nice-number algorithm for readable grid lines at any scale
- Colored chips below controls show pinned combos with × to remove

### compare.html (self-contained)
- "WDW (Orlando) only" checkbox (`state.wdwOnly`) filters the resort list to exclude `NON_WDW_RESORT_IDS` (aulani, hiltonHead, veroBeach, disneylandHotel, grandCalifornian) — that set already existed for the Booking Outlook seasonal-period logic, reused here for the resort filter too
- Owner cost and rental cost columns always show now — there's no "Show rental cost"/"Show owner cost" toggle anymore, since an owner might still rent out points (or vice versa), so hiding either column by default didn't serve anyone. `ownerCol`/`rentalCol` are just `true` in `render()` now rather than reading `state.ownerEnabled`/`state.rentalEnabled` (removed from state)
- Every column header explains itself on hover via `headerTooltipHTML(label, desc, align)`, the same `tooltip-anchor`/`tooltip-card` component used elsewhere — a `.header-tooltip` modifier resets `text-transform`/`white-space`/`letter-spacing` back to normal and widens the card to 210px, since those all otherwise inherit from `.compare-table th` (uppercase, nowrap) straight into the nested tooltip and silently overflow it instead of wrapping
- Every `.tooltip-anchor` on this page also gets a tap-to-toggle fallback (`.tap-open` class, page-level `click`/`Escape` delegate near the end of the script, registered on the CAPTURE phase with `e.stopPropagation()` — see Mobile Responsiveness Guardrail #10, since the column-header tooltips here sit inside a `th[data-sort]`) per `README.md`'s no-hover-only-reveals rule — same "always add on tap, clear every other open one" pattern as `app.js`/`index.html`. `contractvalue.html` needed the same fix but ported to its own bespoke `[data-tip]` + `::after` tooltip mechanism instead, since it predates the shared `tooltip-anchor`/`tooltip-card` component.
- **Personalized Booking Window & Home Resort Risk (2026-09-21):** `analyzeBookingWindow()` turns a signed-in owner's contracts into a per-row read rendered as up to three small chips under the room name (`bookingWindowChipsHTML()` → `roomLineHTML()`, the one insertion point all four render sites — financial table, financial cards, vibe table, vibe cards — already share). Chip 1 is the gate (`11-Month Priority` / `7-Month Switch` / `🚫 Not Bookable`), chip 2 the exact opening date plus a day countdown or `Window open now`, chip 3 a risk flag when history says that room type and date are competitive at that window. Home/non-home comes from `DVCAuth.getUserResortAccess()`, which unions across EVERY active contract — an owner at two resorts gets 11-month priority recognized on both, there is no "primary contract" — and `userContracts` is kept alongside the access sets purely so a home chip can name *which* contract confers the priority (the sets are Sets of resort ids and have forgotten that). Two things it deliberately does NOT do: (1) reuse `monthsUntilCheckIn()`'s whole-month count — that helper was deleted, since "opens in 3 days" and "open now" are different states a month counter can't tell apart, so every time comparison here is day-exact off `daysUntilDate()`; (2) always quote the gating window's historical odds. The bucket it quotes is the NEAREST of `BOOKING_WINDOWS`' 11/7/5/3/1 to how far out check-in actually is, capped by the user's own gate — "largest bucket at or below" had a cliff where a stay 2 months 29 days out floored to 2 and quoted 1-month scarcity for what is, for booking purposes, a 3-month stay. Ties break toward the smaller bucket, since overstating odds is the worse error. The actionable switch strategy ("book {your home resort} at 11 months when it opens {date}, then try to switch here on {date}") rides in the risk chip's own tooltip rather than as a fourth always-visible line — this is a comparison matrix with a row per resort/room type, and three chips is the density ceiling. That strategy is worded differently once the 7-month window is already open ("hold {home} as a fallback and keep checking here"), since there's no future switch date left to aim at; an earlier pass emitted "book X at 11 months now, then try to switch here now," which is nonsense. Chips stack one per line below 768px (`.owner-badge-line` `flex-direction: column`) specifically so every chip's left edge is the card's left edge, which is what makes their fixed `tooltip-align-left` tooltips land inside a narrow card — verified at 385px with zero horizontal overflow and zero tooltip overflow across all 90 chips.
- **Owner Cost selector is contract-aware, not a home-resort picker (2026-09-22, user-reported):** the "Home resort" dropdown that drives the Owner Cost column's dues rate used to always be a plain alphabetical 17-resort `<select>` defaulting to a hardcoded `saratogaSprings` — a signed-in owner had to manually re-pick their own home resort out of that list every visit just to see their real dues, which is exactly backwards for someone who already has a contract on file. `populateOwnerSelector()` (replacing the old `populateOwnerResorts()`) now branches on whether `userContracts` (already populated by this page's own `waitForAuthPersonalization()`, used elsewhere for the booking-window chips above) is non-empty: with active contracts, the dropdown lists THOSE (`state.ownerContractId`, same `"{nickname||resortName} ({use_year} UY, {points} pts)"` label format `app.js`'s own Booking As contract picker uses), defaulting to the first one automatically — the whole fix, no manual resort selection needed. With no active contracts (signed out, or none yet), it falls back to the original alphabetical resort list (`state.ownerResortId`) unchanged, since a prospective buyer with nothing owned still needs a hypothetical "if I owned at X" pick. `getOwnerDuesPerPoint()` is the single resolver both the `#owner-dues-label` display and the actual per-row `ownerCost` calc read, so the two can never disagree about which dues rate is in effect. The label text next to the dropdown swaps too ("Your contract:" vs. "Home resort:") so the control never claims to be picking a home resort when it's actually listing contracts. Repopulating the dropdown (on init, on sign-in/out, on contracts resolving) calls the persistent `<select>`'s cached `._customSelectRender()` rather than re-running `initCustomSelect()` on it — that function isn't idempotent (attaches trigger/dropdown click listeners fresh every call), so a second `initCustomSelect()` call on the same node would double up those listeners.
- **Owner Contract Picker restyled to match index.html's Booking As sheet (2026-09-22, follow-up to the fix above on direct user feedback — the contract-aware dropdown was functionally right but "should look just like" the Booking As picker):** in contract mode, `#owner-resort-select-wrap` (the plain custom-select dropdown) and a new `#owner-picker-trigger` button are two sibling views onto the SAME underlying `<select id="owner-resort">` — `populateOwnerSelector()` toggles which is visible (`display:none` on the other) rather than swapping DOM structure, so `getOwnerDuesPerPoint()`/the change listener needed no changes at all. The picker-trigger opens `#owner-sheet` (`.modal-overlay.sheet-overlay` + `.modal-box.sheet`, tokens.css's shared base mechanics), listing `userContracts` as `.wallet-mini-card`s via `renderOwnerSheetList()` — same gradient-over-art-photo treatment, `{use_year} UY`/`{points}/yr` pills, and card-tap-to-select (`pickOwnerContract()`, sets `.value` + dispatches a real `change` event, same as app.js's own `pickContract()`) as index.html's Booking As sheet. This page doesn't load `styles.css` (fully self-contained) or `dvc-ledger.js`, so two things are deliberately ported rather than shared, and one stat is deliberately omitted: (1) `.picker-trigger`/`.sheet-*`/`.wallet-mini-*` CSS is duplicated from `styles.css` verbatim into this page's own `<style>` block; (2) `resortAccentGradient()`/`resortArtWashCSS()`/`applyResortArtBackground()` are duplicated from `app.js`, with the gradient's hash reusing this page's own pre-existing `resortDotColor()` hash (factored out as `resortAccentHash()`) rather than a second independent hash, so a resort's color matches between its table dot and its wallet card; (3) the card's "AVAILABLE" points stat is skipped — there's no ledger fetch here to back a real number, so every card shows the same honest `?`/UNCONFIRMED fallback state index.html itself uses for a contract with no recorded ledger row, rather than fabricating a balance. The no-contract fallback (signed out, or no active contracts) deliberately keeps the original plain resort dropdown as-is — out of scope for the reported issue, which was specifically about the contract case.

### suggest.html (self-contained)
- Input is a 3-step accordion wizard (`renderWizard()`, `wizardStep` 1-3, plus a `4` "done" sentinel), not a static all-at-once form — Points, Year & Travel Window → Room Type & Party Vibe → Stay Length & Scope. Only the active step's body is ever in the DOM; completed steps collapse to a tap-to-reopen summary row (`collapsedStepHTML()`), steps not yet reached render dimmed (`upcomingStepHTML()`). All wizard state lives in one `wizardData` object (not read from scattered DOM fields at submit time) — `runSearch()` reads directly from it. `wizardStep = 4` is a "done" sentinel meaning "collapse all three steps," not a real step; every `s < wizardStep` check in the render loop is naturally true for all of 1-3 at that point, so no special case is needed to fully collapse the wizard after a search.
- Year and Travel Window live in the SAME step (step 1), not separate steps, and their choices constrain each other — `availableMonthsForYear(year)` hides months before "today" whenever the selected year is the current real year (a future year has no such restriction), so picking 2026 in September never offers April. Changing the year `<select>` fires a real "change" event (`dvc-ui.js` dispatches one from the custom-select, same as a native select) that re-renders step 1 and resets `wizardData.month` to "any" if the previously-picked month is no longer valid for the newly-selected year. `findFeasibleStays()` has the same guard independently (`TODAY_STR` filter on generated `startDates` when `year === currentRealYear`) so "Any month" for the current year can't generate already-past candidate dates either — the UI restriction and the search restriction are two separate, deliberately redundant checks, not one relying on the other.
- `minNights`/`maxNights` default to **5/7**, not 1/14 — the old 14-night ceiling meant almost every resort/room combo maxed out its greedy `maxNightsWithinBudget()` extension into one long "marathon" stay, which is what actually caused the old ~128-result bloat, not a flaw in the underlying feasibility algorithm (unchanged, and still documented as the correct max-nights answer for a single continuous stay). Capping the default window to a realistic vacation length was sufficient; no change to `findFeasibleStays()`'s search logic was needed.
- Deliberately does NOT use the dual-view Mobile Card Feed pattern (table for desktop, cards for mobile below a breakpoint — see guardrail #7 and `contractvalue.html`) — results render as `.result-card`s at every viewport, with no `<table>` in the DOM at all (see the 2026-09-18 mobile-first overhaul). This page's rows only ever carry 3 real metrics (Nights, Points Used, Points Left) plus a date range, so unlike `contractvalue.html`'s dense 8-column ranking table, there's no desktop dense-scan use case a table would serve that the card stack doesn't already cover. Sorting is one pill toolbar (`buildSortToolbarHTML()`, `.sort-pill` buttons) shared by all viewports — no header-click-to-sort affordance exists since there's no table.
- Results default to sorting by `pointsPerNight` ascending ("Best Value") rather than `nights` descending — each result also carries a `pointsPerNight` field (`pointsUsed / nights`, computed in `findFeasibleStays()`) purely for this sort; it's not shown as its own card metric since the card's stats block is deliberately just the 3 fields above. Only the top `DEFAULT_RESULT_LIMIT` (20) render initially regardless of total match count — a "Show all N stays" button (`showAllResults` flag) reveals the rest on tap rather than hiding them permanently, so "curated by default" never means "data withheld."
- `renderResults()`'s empty state (`buildEmptyStateHTML()`) is context-aware: it reads `lastParams` (points/minNights/scope/category from the most recent `runSearch()`) to only surface a "try this instead" recovery button when that filter is actually the kind of thing loosening it would help (e.g. "Search all resorts" only appears if scope isn't already "all"). Its buttons mutate `wizardData` directly and re-call `runSearch()` rather than routing back through the wizard steps.
- The Points Year field uses the shared `.custom-select` component (`tokens.css` + `dvc-ui.js`, same markup/JS as `index.html`'s Resort/Room Type pickers) instead of a bare `<select>`, per the native-select ban in `README.md`. Because step 1's whole body is rebuilt via `innerHTML` on every `renderWizard()` call, `window.DVCUI.initCustomSelect()` is re-run on the fresh `<select>` node each time step 1 renders — unlike `index.html`'s persistent selects, which instead call the cached `._customSelectRender()` to repopulate without re-init. The "Resorts I can book" scope toggle in step 3 is a plain two-way toggle (all vs. access), not three-way — the old "My home resort(s) only" option was dropped in the mobile-first redesign.
- "View This Stay" (in `renderResults()`) hands off to `index.html` two ways: the existing one-shot `dvc_calendar_state`/`dvc_return_to_calendar` pair (consumed and cleared by `app.js` on load, restores the actual calendar selection) AND a separate, NOT one-shot `dvc_suggest_state`/`dvc_suggest_return` pair that `index.html`'s "Back to Suggest a Stay" banner (`styles.css`, wired in `app.js`) uses to link back here. `dvc_suggest_state` holds `{ wizardData, sortKey, sortDir, showAllResults }` — not `lastResults` itself, since results are always deterministically recomputed from `wizardData` via `runSearch()` on restore rather than duplicating storage of derived data. Both keys are consumed (read once, then removed) the moment suggest.html successfully restores from them; a fresh "View This Stay" click re-sets both for the next round trip regardless.
- **Gemini review fixes (2026-09-21):** a Gemini-authored review of this page against 5 test cases (stranded points, party-size/room-capacity filtering, weekend/weekday point stretching, home-vs-7-month contract awareness, month-fallback recovery) found weekend/weekday stretching already worked as an emergent property of the "maximize nights" search, but flagged four real gaps, all fixed this pass:
  - **Party size filter:** step 3 ("Room Type & Party Vibe") gained a `wizardData.partySize` chip row (4/5/7/9/12+, `roomMatchesPartySize()`) filtering on the `sleeps` field every room type in `data.js` already carried but this page never read — e.g. a party of 5 now correctly excludes Copper Creek/AKV's sleeps-4 standard studios while keeping Riviera/BWV/BC/Poly's sleeps-5 studios with the 5th-sleeper pull-down.
  - **Stranded points:** `findFeasibleStays()`'s per-resort/room search still maximizes nights first, but a tie on night-count now breaks toward whichever start date actually spent more of the budget (`nights === bestNights && total > bestTotal`) instead of keeping whichever start date was found first — a same-length stay that happened to use fewer points no longer silently wins over one that used more. Each card also states this plainly now ("Uses 66 of 67 pts · 1 leftover", `.result-card-leftover`) rather than leaving the two Points Used/Points Left tiles to be read together. Two new sort options round this out: "Least Leftover" (`pointsLeft` ascending) and "Best Cash Value" (`cashValuePerPoint` descending, via a new `cashValueForStay()` using `getCashRateWithFallback()` — null-safe in `sortResults()` since non-WDW-estimate resorts can still return `null`).
  - **Contract awareness / lockout risk:** the real risk Gemini flagged — a stay suggested well outside a user's actual booking window (e.g. 10 months out at a resort only reachable at 7 months) rendering identically to a home-resort suggestion — is fixed by `analyzeBookingAccess()`, a trimmed port of `compare.html`'s `analyzeBookingWindow()` (gating half only; this page doesn't load `availability_data.js` so it skips the historical-risk quote). Every card gets a badge (🏠 Home Resort / 7-Month Switch / 🚫 Not bookable) plus the exact window-open date and day countdown, computed fresh at render time off a page-level `userResortAccess` (now set on every auth change, not just when `scope === "access"`) so it can never go stale across a sign-in that happens after a search already rendered. The "Resorts I can book" scope toggle still narrows the search itself; the badge is independent so switching scope back to "All resorts" never hides which suggestions are actually reachable.
  - **Month fallback:** `buildEmptyStateHTML()` now offers "Search any month instead" (clears `wizardData.months`) whenever the search was month-constrained, ahead of the points-bump suggestion — a slim target-month search gets a calendar fix offered before a wallet one. `lastParams` was extended to carry `months`/`partySize` so the empty state can reason about them.

### contractvalue.html (self-contained)
- Input is the same 4-step accordion wizard pattern as `suggest.html` (`renderWizard()`, `wizardStep` 1-4, `5` = "done" sentinel) — Purchase Intent & Channel → Target Point Budget → Primary Room Category → Usage Horizon & Vibe — replacing what used to be a single static form showing every input (including "advanced" ones like closing cost and dues growth) at once. All the actual valuation math (`computeRow()`, `sortRows()`, `bestIndices()`, `medianRoomPoints()`, the breakeven-horizon loop) is UNCHANGED from before this rewrite — only the input/results presentation layer changed. `computeRow()` reads `wizardData.purchaseType`/`wizardData.category` directly (no more standalone `purchaseType`/`category` module variables).
- Closing Cost and Dues Growth are NOT wizard steps — they default to 1500/4.0 (with the same resale/direct auto-adjust rule as before: only touched if still at either default, so a manual edit survives toggling purchase type back and forth) and live in a collapsed-by-default "Adjust assumptions" panel (`showAdvanced`, `.advanced-panel`) inside the results view instead, per the redesign's explicit goal of not surfacing advanced financial parameters until asked for.
- Results lead with `.match-card`s (`pickHeroCards()`), NOT the full 17-resort ranking — 2-3 curated picks, each won on a DIFFERENT metric so the badges are each independently true rather than three restatements of the same ranking: "Best Overall Value" (lowest `costPerPointYear`, the primary metric), "Best Nightly Cost for {category}" (lowest `nightlyRoomCost`, genuinely category-dependent unlike Cost/Pt/Year), "Best Fit for {N-Year Plan / a Full-Length Contract}" (lowest `effectiveCostPerPointYear`, personalized to the wizard's vacation-years answer). Each slot skips any resort a prior slot already claimed so the same resort isn't showcased three times over.
- The full ranked table+cards (`.results-table-wrap`/`.results-cards`, the ORIGINAL Mobile Card Feed pattern this page established per guardrail #7) still exists verbatim and is still how every one of the 17 resorts gets compared side by side — it's just collapsed by default behind a "View All N Resorts" toggle (`showAllResorts`) now, per the redesign's progressive-disclosure requirement. `.price-input`/`.price-reset` editable buy-in overrides work identically in match-cards, the full table, AND the full card list — all three render locations share the exact same class names/`data-resort` attributes, so the one existing `document.querySelectorAll(".price-input")`-based wiring in `renderMatches()` picks up all of them for free.
- `restrictedBadgeHTML()` is a small helper (returns the same `<span class="restricted-badge" data-tip="...">` markup) shared by match-cards, the full table, and the full card list — introduced during this rewrite to avoid a third copy of that long explanatory string. The old standalone `.restricted-legend` paragraph (always-visible, explaining what "resale-restricted" means before a user ever saw a badge) was removed — that explanation now lives as `.wizard-hint` text in step 4 right where the hide-restricted toggle is decided, plus each badge's own tap/hover tooltip once results are showing.
- `METRIC_TIPS` (a plain object, `pricePerPoint` the one entry that's a function of `purchaseType` rather than a static string) holds every column's explanatory tooltip text ONCE, shared between the full table's `colHeader()` info-icons and the match-cards' own `.info-icon` tooltips (`matchCardHTML()`) — added 2026-09-18 after a user question revealed match-cards had NO explanation at all for Cost/Pt/Year or Est. Nightly Cost, since only the full table (behind the "View All Resorts" toggle most users never open) had info-icons. Est. Nightly Cost's tip text is explicit that it's an OWNERSHIP cost figure (`costPerPointYear × median points-per-night for the category`), NOT Disney's cash rack rate or a rental-market estimate — that distinction wasn't previously stated anywhere and was the actual source of the question. Step 4's field label reads "How Long Do You Plan to Keep This Contract" (not "Years You'll Keep Vacationing" as originally shipped) per direct user feedback that the original phrasing was confusing next to "Usage Horizon."
- **`averageDuesPerPoint(years)` (2026-09-18 correctness fix, not just a docs/tooltip fix):** `costPerPointYear` and `effectiveCostPerPointYear` used to add flat, un-grown `currentDuesPerPoint` regardless of the Dues Growth input — a user correctly pushed back that a "true cost of ownership" figure that can't distinguish a resort with fast-growing dues from one with flat dues isn't measuring what it claims to. Both metrics now add the OUTPUT of `averageDuesPerPoint(years)` instead — dues compounded at `wizardData.duesGrowth` and averaged over the metric's own amortization window (`yearsRemaining` for `costPerPointYear`, `vacationYears` for `effectiveCostPerPointYear`) — so raising Dues Growth now visibly moves Cost/Pt/Year, Est. Nightly Cost (derived from it), Effective Cost/Pt/Year, and potentially which resort `pickHeroCards()` picks as "Best Overall Value." The horizon CHOICE itself (years-remaining vs. the user's own vacation-years) is still deliberately different between the two metrics for the same reason as before (see the `effectiveCostPerPointYear` code comment) — only the "dues never grow" assumption was the actual bug.
- **Cash Ratio restored (2026-09-18):** the file this rewrite started from had already dropped a "Cash Ratio"/"Rent Ratio" pair that was then still live in production (the last git-committed version at the time, `git log -- contractvalue.html`; the app was on `dvcalc.app` then, dvccompanion.com now) before this session ever touched it — the wizard rewrite was built on top of that already-ratio-less state, not the thing that removed it. Restored on explicit user request as Cash Ratio ONLY (not Rent Ratio — the user chose to skip the "vs. renting instead" comparison and keep just "vs. paying cash rack rate"), computed as `totalValue / lifetimeOutlay` where `totalValue` compounds a new `wizardData.pointValue`/`wizardData.valueGrowth` assumption (defaults $35/pt/yr @ 5%/yr, matching production's own historical defaults) over `vacationYears` the same way `totalDues` already did. Higher is better here, unlike every other metric on the page — `bestIndices(..., "high")` for its checkmark, where every other column uses `"low"`. Shows as a bold "N.NNx" on match-cards (3rd stat, per explicit request to keep it alongside Cost/Pt/Year rather than replacing it), in the full table (`cashRatio` column, sortable), and in the mobile card feed — NOT one of `pickHeroCards()`'s three badge-picking criteria, by design (kept those three as-is rather than growing the hero-card count or bumping an existing badge). The two new Vacation Value inputs live in the same collapsed-by-default "Adjust assumptions" panel as Closing Cost/Dues Growth, per explicit user choice over adding a new wizard step.
- **Gemini review fixes (2026-09-22):** a Gemini-authored review against 4 checks (forward amortization to real deed-expiration dates, compounding dues, resale/direct restriction handling, cash-vs-rental/cash-vs-rack-rate comparisons) confirmed the first two were already correct — `yearsRemaining = contractExpirationYear - currentRealYear` genuinely amortizes forward off `data/resort_investment.js`'s real 2042/2054/2070/etc. deed dates, and `averageDuesPerPoint()` genuinely compounds rather than flat-multiplying — but found real gaps in the other two, fixed this pass:
  - **Direct Premium:** `computeRow()` now also returns `directPremiumPerPoint`/`directPremiumPct`, always computed off the two BASE listing prices in `resort_investment.js` (never a `priceOverrides` edit, which only applies to whichever single price the row is currently showing — mixing an edited price on one side with a stock default on the other would make the gap meaningless). `directPremiumCaptionHTML(r)` renders it as a small caption under the editable price input at all three render sites (match-card/resort-card/table, same triplication every other per-row fact on this page already has) — "Direct: +$123/pt (+102%)" in resale mode, "Resale: −$123/pt (−51%)" in direct mode.
  - **Resale interchangeability, stated positively:** previously a legacy resort's resale interchangeability was only ever implied by `restrictedBadgeHTML()`'s absence — a buyer browsing Old Key West resale saw no explicit confirmation it can reach the other 13 original resorts, only silence where Riviera/DLH/Cabins would show a warning. `accessBadgeHTML(r)` is the new single call site for both badges (replacing the old `r.isRestricted ? restrictedBadgeHTML() : ""` at all three render locations): resale mode shows `.legacy-badge` "7-mo eligible" for the 14 unrestricted resorts and the existing `.restricted-badge` for the 3 home-only ones; direct mode shows neither (direct purchases are never home-only-restricted, so there's nothing to confirm). `renderPerksCallout()` is now genuinely mode-aware instead of only ever firing in direct mode — its new resale-mode branch states the "14 original/legacy resorts book each other at 7 months, except Riviera/DLH/Cabins" fact in general terms, pointing to the per-row badges and `directPremiumCaptionHTML()` captions for the resort-specific version of both facts.
  - **Rental parity — deliberately NOT a new metric:** Gemini's "instant $/pt margin vs. the ~$20/pt rental market" is the same comparison the 2026-09-18 Cash Ratio restore (above) explicitly chose to skip in favor of Cash Ratio alone. Rather than re-open that decision, `METRIC_TIPS.yearsToBreakeven` gained one sentence naming the actual `RENT_POINT_VALUE` ($20/pt) the breakeven calc already uses internally but never stated, plus the instant-margin read ("if Cost/Pt/Year is already under $20, you're beating the rental market from year one") — a tooltip-text addition, not a new visible column or ranking criterion.

### itinerarycompare.html (self-contained)
- **Contract-aware decision rows (2026-09-21):** three rows were added directly beneath Room Type/Dates. **Home Priority & Window** checks the itinerary's resort against `window.DVCAuth.getUserResortAccess()`'s union across every active contract (not a single "selected" contract like index.html's Booking-As card) — a home match shows the exact 11-month opening date, a reachable-but-not-home resort shows the 7-month switch date plus a live day countdown, and a resort no contract can reach shows "Not Bookable." **Availability Risk (7mo)** pulls the real 7-month score from `data/availability_data.js` — the same DVC Field Guide data the calendar's own Booking Outlook card uses — and collapses its 5-tier scale into a 3-tier 🟢/🟡/🔴 read; shown for every itinerary regardless of contract ownership, since 7-month odds matter to renters too. **Point Feasibility** pools every active contract's balance for the use-year cycle covering the trip via `window.DVCDates.currentUYYear()` generalized to the check-in date instead of "today" (same fallback-to-full-allotment behavior as `app.js`'s `getCurrentYearRow()` when no row exists for that cycle yet), with borrow headroom modeled as ceiling-minus-already-borrowed the same "elastic capacity" way `app.js`'s 11-to-7 Swap Simulator does (see that section above for the bug this avoids repeating). All three need `dvc-dates.js`/`dvc-ledger.js`/`data/availability_data.js` loaded alongside this page's existing self-contained data scripts. Home Priority and Point Feasibility are both omitted entirely (not blanked per-cell) with one explanatory note when the signed-in user has zero active contracts — Availability Risk still shows either way, since it needs no contract.
- **Mobile layout (2026-09-21):** a stacked "one full card per itinerary" mobile redesign was built, screenshotted against a real ~390px viewport, and rejected on user feedback — it eliminated horizontal overflow, but a comparison feature whose entire point is seeing every option at once shouldn't force scrolling through one option at a time to get there. The shipped fix keeps ONE `<table>` at every width instead: below 700px (this page's existing breakpoint, see `.picker-row`) the table goes `table-layout: fixed` with an explicit row-label column width and every cell wrapping at word boundaries, with font/padding shrunk just enough that up to 3 itinerary columns stay visible side by side with zero horizontal scroll — verified against a real 390px viewport with both 2 and 3 itineraries selected. The one accepted rough edge at 3-up density is a genuinely long unbroken word (e.g. "Boardwalk/Preferred") wrapping mid-token. Capping selection at 2 itineraries (simpler layout, no density tradeoff) was considered and explicitly rejected 2026-09-21 in favor of keeping the existing "up to 3" capability, since 3 columns were confirmed to actually fit without scrolling. A real CSS gotcha surfaced building this: the compact ruleset's `.compare-table tbody th { white-space: normal }` override was first placed right after `.compare-table`'s own base rules and was silently beaten by the pre-existing unconditional `.compare-table tbody th { white-space: nowrap }` rule further down the same stylesheet — a `@media` block doesn't add specificity, and the cascade tiebreaks equal-specificity rules by source order, not by which one happens to be gated by a media query. Moving the compact block to the very end of the `<style>` (after every rule it needs to beat) fixed it; any future override added to this page's compact-mobile ruleset needs to stay last in the stylesheet for the same reason.

### dvc-badges.js (Trophy Room — shared by badges.html, badges_info.html, trips.html)
- **Gemini review fixes (2026-09-22):** a Gemini-authored review of the Trophy Room against 4 checks (Odyssey geographic groupings, financial/contract milestones, booking-craft variety, locked-state and celebration UX) confirmed most of the system correct — House Money genuinely amortizes purchase + closing + compounded dues, Points Steward's 95% top tier is the "Use Year Disciplinarian" check, tier enamel is visually distinct across all four rungs, and `applyRarityStats()` suppresses rarity below 5 members rather than inventing a percentage. Four real gaps were fixed, all covered by the new `tests/dvc-badges.test.js` (22 tests, plain `node --test`, loaded via `vm.runInThisContext` since this file is a `window.DVCBadges` IIFE and can't be `require()`d the way `dvc-ledger.js` is):
  - **The Odyssey awarded tiers it hadn't earned (the real bug).** `groupsCoveredCount` used `.some()`, so visiting ONE resort in a region counted the whole region as covered — while the tier *labels* indexed `ODYSSEY_GROUPS` by that count. A member whose only stay was Riviera was therefore shown "Tier 1: The Monorail Loop", with a ✓ next to three resorts they'd never visited, the region they actually finished left unchecked, and a next-step nudge telling them to go do what they'd already done. Two changes fix it together: a region now requires `.every()` resort in it (which is what the tier `requirementLabel`s — "Stay at Bay Lake Tower, Grand Floridian, Polynesian" — had claimed all along), and tier labels became *counting* labels ("One Region"/"Two Regions"/"Three Regions"/"Global Citizen") rather than region names. Counting tiers need counting labels: the ladder is "how many regions", so no single region name can ever be a true label for a rung. Which regions are done is carried by `valueLabel`/`nextStepText` instead, where it's always accurate. This also keeps `applyBadgePersistence()`'s sticky-floor path honest — it rebuilds `tierLabel` from `tiers[storedTier - 1].label`, so with region names there it could have re-introduced the same false claim from stored data alone.
  - **Odyssey progress is now per-region, not global.** `valueLabel`/`customProgressPct`/`nextStepText` describe the region *nearest completion* (fewest resorts missing, ties by declaration order) rather than a global `N of 17` count that was identical whether you were one resort from finishing a region or seventeen — e.g. "3 of 4 The Wilderness resorts visited" + "stay at Fort Wilderness Cabins to finish The Wilderness". Once all three regions are done the badge switches to the global count, since Global Citizen genuinely is the only thing left. Fort Wilderness Cabins was added to The Wilderness (making it a 4-resort region); Old Key West and Saratoga Springs stay deliberately region-less — they'd form a 2-resort Disney Springs group far easier to finish than the others, and `TIER_CLASSES` only has 4 rungs to spend — so they count toward Global Citizen only.
  - **Points Whale started too high to fire.** Tiers moved from 250/400/600/1000 to **100/200/500/1000** ("Century Club" is the new first tier), because a 250-point floor meant the single most common real contract size — a 150-point direct minimum or a small resale — unlocked nothing at all. Lowering thresholds is always safe for a sticky badge: it can grant a tier, never take one back.
  - **Treehouse Villas now count as Grand Villa.** `data.js` names the room "Three-Bedroom Treehouse Villa", so `isTrueGrandVillaTrip()`'s `"grand villa"` substring missed it — despite it being Saratoga Springs' largest, rarest, sleeps-9 unit, at a resort with no actual Grand Villa for it to be losing out to. Villa Royalty (the broader 2BR+ badge) is unchanged and still distinct.
  - **Locked special badges stated no criteria.** `badgeProgressHTML()` returns `""` for `kind === "special"`, so all 17 locked special badges rendered as nothing but a name and the word "Locked" — criteria were modal-only, while every tiered badge stated its own on the tile face via its progress line. `evaluateSpecialBadge()` now takes a terse `requirement` (falling back to `detail`), rendered by the new `badgeRequirementHTML()` into `buildTrophyTileHTML()`. `requirement` is the tile-sized restatement (a test caps it at 60 chars); `detail` stays the long modal version.
- **Known gap, deferred: event badges are invisible to rarity stats.** `badge_rarity_stats()` filters `where ub.tier > 0`, but `increment_badge_event()` only writes `event_count` — `tier` keeps its `default 0` — so all six `evaluateEventBadges()` badges never get a "N% of members have this" line. It fails closed (an absent line, never a fabricated number, consistent with `applyRarityStats()`'s own `totalMembers < 5` suppression), so it's deliberately deferred until there's a real member base to verify a fix against. Full writeup and the two candidate fixes: [`docs/badge_rarity_todo.md`](docs/badge_rarity_todo.md).
- **Unlock celebration on trips.html (2026-09-22, same review):** the Trophy Case preview was removed from this page 2026-09-17 in favor of one canonical home on badges.html — which left logging a trip that unlocks a badge producing no signal whatsoever. `detectNewlyUnlockedBadges()` diffs `DVCBadges.unlockedBadgeIds()` between renders and `buildUnlockBannerHTML()` shows a one-time banner linking through to the Trophy Room. Three deliberate constraints: itineraries are passed as `[]` (this page never fetches them) *on both sides of the diff*, so itinerary-driven badges never produce a false "just unlocked" rather than being wrongly credited to a trip; `evaluateUserBadges()` is called directly and NOT through `applyBadgePersistence()`, which fires Supabase upserts — this is a read-only probe running on every render, and badges.html stays the one place that persists tiers; and it only fires when `justLoggedTripId` is set, with `lastUnlockedBadgeIds` starting `null` so the first render after sign-in establishes a baseline instead of celebrating the member's entire existing collection at once. The banner is show-once (cleared alongside `pendingWarning`) and wrapped in try/catch — a badge-evaluation failure must never take down the page's real payback/trip content.
- **11-Month Sniper, redefined to something derivable (2026-09-22):** Gemini's literal spec — "awarded for booking high-demand inventory right as the home resort priority window opens" — is not computable: `trips` records `check_in`/`check_out` but no *booking* date, so nothing in the schema knows when a reservation was actually made. Rather than add a booking-date column, the badge now rewards the **planning** behavior that stands in for it, on explicit user direction: picking a stay on the calendar whose check-in is 10–11 months out. That's the one-month stretch in which that specific stay's 11-month window opens, so being in the app looking at it then *is* the sniper's actual behavior. It's the 6th event-count badge (`dvc-track.js` → `incrementBadgeEvent()` → `evaluateEventBadges()`), tiered 1/5/15, fired from `app.js`'s `trackElevenMonthSniper()` at both stay-completion points in `handleDateClick()` (normal and split-stay). Three things worth knowing:
  - **The home-resort gate is load-bearing, not incidental.** The event only fires when the browsed resort is a home resort on one of the owner's own *active* contracts. 11 months is purely a home-resort privilege — eyeing a non-home resort that far out isn't sniping, it's a stay nobody can book yet — so without the gate the badge's name would be a lie about its mechanic, the same standard that got Holiday Chaser renamed to Main Event and Coast to Coast to Two-Coast Deeds. An owner with no active contracts never fires it, for the same reason. **An owner can hold deeds at several resorts and has 11-month priority at every one of them**, so the gate reads the UNION of `getUserResortAccess(activeContracts, allIds).homeResortIds` (the same union `compare.html`/`itinerarycompare.html` use — there is no "primary" home resort and no "currently selected contract" involved) rather than testing one contract at a time. Only `homeResortIds` may gate it: a *direct* contract puts every resort into `sevenMoResortIds`, and 7-month reach is precisely what an 11-month snipe is supposed to beat. `tests/eleven-month-sniper.test.js` covers the multi-deed, direct-contract, home-only-resale, inactive-contract and no-contract cases against the real `getUserResortAccess()`.
  - **The date math lives in `dvc-dates.js`, not `app.js`.** `isInFinalWindowMonth(checkIn, months, from)` and `monthsFromDate(months, from)` are new there (already loaded on `index.html`) specifically so the boundary behavior is unit-testable without app.js's DOM dependencies. Far edge inclusive (exactly N months out is the day the window opens — the sniper moment itself), near edge exclusive, so adjacent windows can never both claim one date. `monthsFromDate()` deliberately keeps `Date.setMonth`'s ROLLOVER semantics (Jan 31 + 1 month → Mar 3) rather than clamping, matching `app.js`'s existing `monthsFromTodayCutoff()`/`monthsBeforeCheckIn()` — a lone clamping helper would put two different "11 months from now" dates in the same app. A test pins that rollover so a future "fix" can't silently desync them.
  - **A `suggest.html`/`compare.html` handoff deliberately does NOT fire it.** The restore path at the bottom of `app.js` rehydrates a stay via `Object.assign(state, restored)` and never routes through `handleDateClick()`, so the event only fires on a genuine pick made on the calendar itself. Confirmed as intended 2026-09-22 (a stay handed over from another page wasn't picked here, and re-firing on every page load would inflate the count for one decision) and noted in `trackElevenMonthSniper()`'s own comment so a future refactor doesn't quietly change it.
  - **Two Snipers is a deliberate pair, not the collision it looks like.** 7 and 11 months are DVC's two real booking windows, so the badges read as a set (different icons: 🎯 vs 🔭, different mechanics: a logged away-from-home stay vs. a tracked planning action). Contrast Bicoastal vs. the old "Coast to Coast", renamed because those two shared a name *shape* with no distinguishing principle behind them.

---

## Contract Manager Flow (2026-09-22 new-owner UX review)

- **Active Member gate** (`auth.js`, `MEMBERSHIP_GATE_ENABLED`): implements `docs/subscriptions_plan.md`'s tier split. `hasMembership()` (cached per session, refreshed whenever `getSubscription()` runs) treats `active`/`trialing`/`past_due` as members. Enforced in auth.js's own data layer: owner-data reads (`getContracts`, `getContractYearPoints`, `getTrips`, `getItineraries`, `getUserBadges`) return `[]` and writes return `MEMBERSHIP_REQUIRED_ERROR` for non-members, so calendar/compare/suggest personalization falls away without per-page code. Owner pages (account, trips, itineraries, itinerarycompare, badges, home) render `renderMembershipGate()` (a "Start 7-day free trial" card on the page's own `.gate`) instead of their content. `send-banking-reminders` skips non-members too (same constant; needs a redeploy). Client-side only: RLS doesn't check membership yet. Turn the flag off until live-mode Stripe exists, since nobody can pay before then.
- **Logging a trip takes its points out of the ledger** (`dvc-trip-deduct.js`, `tests/trip-deduct.test.js`): Log a Trip / Record a Booking shows "Take these points out of my balances" with a per-contract preview. Each allocation draws from the use year covering check-in, holding → banked → borrowed → current (same order as the calendar's Suggested draw), written after the trip saves via `upsertContractYearPoints()`. Skipped (with a reason) when that use year has no confirmed balance, has ended, or is short. New trips only: editing never touches balances, since it can't know whether the original save deducted. Default on; the hint says to leave it off if the entered balance already counts the booking. The calendar button is "Record This Booking", and the modal is titled "Record a Booking" for a future check-in.
- **House Money projection ignores trip pace until it covers a year** (`dvc-owner-value.js`): trip history only blends into the forecast once logged owned points reach one year of the active contracts' annual points (`velocitySource` `"baseline-early"` before that). Before this, logging a first small trip moved the payback date later (2035 → 2043 in the review).
- **Calendar defaults to the owner's contract**: a fresh visit (no restored/handed-off state) selects the first active contract and jumps to its home resort (`applyOwnerDefault()`), and Owner Cost follows whichever contract is selected (`syncOwnerResortToContract()`) instead of the hardcoded Saratoga Springs default. The contract's "✓ Bookable" line turns into an amber caveat when the stay's current booking window (`currentBookingWindowKey()`, nearest of 11/7/5/3/1 months, same rule as compare.html) rates Low/Not Likely in Booking Outlook. "Split points across contracts" is only offered with 2+ eligible contracts that have confirmed balances; otherwise it links to the missing balance.
- **My Contracts**: use-year tabs show their real date range (`useYearRangeLabel()`), Record banking warns (without blocking) once that cycle's banking deadline has passed, a one-tap email-reminder offer shows while reminders are off (dismissible, localStorage), the Add Contract resort picker sorts/searches by shorthand + abbreviations (`RESORT_ABBREVIATIONS`), a points chip only auto-advances after a use year was actually tapped, and Danger Zone is a collapsed `<details>`.
- **7-Month Sniper** now requires the away stay's funding contract to actually reach that resort at 7 months (`getUserResortAccess([contract]).sevenMoResortIds`), so a stay at a resort that contract can't book no longer unlocks it. It still can't tell when a stay was booked (trips have no booking date).

## Mobile Responsiveness Guardrails

Checklist for any new page or component, established 2026-09-18 after two rounds of real mobile bugs (calendar/legend/controls-bar overflow on index.html, then crushed multi-column forms and an illegibly-squeezed table on contractvalue.html/suggest.html/itinerarycompare.html):

1. **Multi-column form grids collapse to 1 column, not 2, under `max-width: 700px`.** An intermediate 2-column tier still crushes a labeled dropdown or a 3-4-button toggle into an unreadable sliver on a real phone. `grid-template-columns: 1fr` is the only mobile value that's actually safe — see `contractvalue.html`/`suggest.html`/`itinerarycompare.html`'s `.form-grid`/`.picker-row` rules.
2. **A horizontal-scroll table is only acceptable for a genuinely narrow table (≤5 columns)** — `.amort-table` (5 cols) and `.compare-table` are fine with `overflow-x: auto` + `table-layout: auto` (content width naturally forces the scroll). **A wide/dense data table (6+ columns of ranked or comparison data) gets a Mobile Card Feed instead, not a scrolling table** — see rule 7. A multi-column spreadsheet is the wrong shape for a phone screen no matter how it's scaled or scrolled; squeezing/scrolling it was tried twice on `contractvalue.html`'s "Ranked by Value" table and rejected both times as still feeling like "a zoomed-out desktop website."
3. **Never pair `table-layout: fixed` with `width: 100%`** on any table, mobile or not — fixed layout divides that 100% among every column regardless of viewport, silently crushing each one instead of scrolling or wrapping.
4. **A flex container switching to `flex-direction: column` on mobile also needs `align-items: stretch`** if any child relies on `width: auto` — a desktop `align-items: flex-start` (common for top-aligned sidebar layouts) lets that child shrink-to-fit its own widest content instead of the viewport once it's stacked, which silently reintroduces horizontal overflow. This bit `index.html`'s `.main-layout`/`.summary-side.expanded`.
5. **Never rely on `Ctrl+Shift+M` alone to check this** — it only toggles DevTools' device toolbar when DevTools already has focus; otherwise Chrome intercepts it as the profile-switcher shortcut. Open DevTools (F12) first, then the device toolbar icon or the shortcut.
6. **Before calling a page mobile-ready, verify at ~375-390px width in TWO states**: the page's default/empty state, and after completing its main interactive flow (selecting a stay, submitting a form, opening a modal) — new wide elements often only appear once real content renders (this is exactly how the Cost Comparison/Stay Insights overflow above was missed initially). The check itself: `document.documentElement.scrollWidth === document.documentElement.clientWidth` should hold in both states; if it doesn't, walk `document.querySelectorAll('*')` for any element whose `getBoundingClientRect().right` exceeds the viewport width to find the actual offending element rather than guessing.
7. **Mobile Card Feed pattern for wide/dense tables** (established on `contractvalue.html`'s "Ranked by Value", 8 columns): build BOTH the `<table>` and a parallel `.{page}-cards` div from the same sorted data array in one render function, and let a single `@media (max-width: 768px) { .results-table-wrap { display: none; } .results-cards { display: flex; } }` rule decide which one shows — never a JS-side branch on viewport width, and never a second copy of the render logic. Each card: a header (primary identity + status badges), one **hero metric** in a large bold callout (the same single column the desktop table highlights as "best," e.g. with a checkmark/color), then the remaining metrics in a `grid-template-columns: repeat(2, 1fr)` key/value micro-grid — never another scrolling row of columns. Any interactive cell (an editable price input, a reset button) reuses the exact same class name and `data-*` attributes in both the table and card markup, so existing `document.querySelectorAll(".foo")`-based event wiring picks up both copies automatically — don't write a second wiring path for the card version. If the table has column-header click-to-sort, add a mobile-only `<select>` (`.mobile-sort-control`, hidden on desktop, shown under the same 768px query) that sets the same `sortKey`/`sortDir` state and re-renders, since the card view has no header cells to click.
8. **A hidden tooltip/popover can inflate `scrollWidth` even though nothing is visibly overflowing.** `.tooltip-card` (tokens.css) sits at `visibility: hidden` until hover, not `display: none` — hidden-but-present elements still generate a layout box, and if that box's TEXT inherits `white-space: nowrap` from an ancestor that set it for its own unrelated reasons (`.wallet-card-deadline-pill` needed nowrap for its own one-line label; `.tooltip-card` nested inside it inherited that too), a normal sentence-length tooltip renders as one ~800px+ invisible line and silently adds horizontal scroll to the whole page. `getBoundingClientRect()` on the tooltip itself won't catch this (its reported box still looks small/contained) — only `element.scrollWidth` vs `offsetWidth` reveals it. `.tooltip-card` now sets `white-space: normal` explicitly so this can't recur, but the general lesson: any shared hidden-until-interaction component should set every inheritable text property (`white-space`, `text-align`, etc.) explicitly rather than trusting whatever ancestor it happens to get nested inside next.
9. **Grid, not flex, for a row of equal-width stat boxes.** `flex` with no explicit `flex-basis`/width sizes each child to its own content — for a box wrapping an `<input>`, that resolves to the input's own default browser intrinsic width (~170-190px), not something that shrinks for a phone. Three such boxes fit fine on desktop and wrap into oversized single-column blocks on mobile ("looks like a form" was the actual bug report — `account.html`'s Banked/Current/Borrowed ledger). `grid-template-columns: repeat(3, 1fr)` forces every item into an even, viewport-relative share of the row at any width and fixes this without a media query at all.
10. **A `.tooltip-anchor`/`[data-tip]` tap-to-toggle delegate must run on the CAPTURE phase and call `e.stopPropagation()`, not just listen on bubble.** Found on `compare.html`/`contractvalue.html`: their column-header info-icon tooltips sit inside a `th[data-sort]` that has its own click-to-sort listener. A bubble-phase `document.addEventListener("click", ...)` delegate runs AFTER that `th` listener already fired (bubbling goes target → th → document), so by the time the delegate tries to flag the tapped icon with `.tap-open`, the `th`'s own handler has already re-rendered the table and detached that exact node — the tap-open class lands on a dead element the live DOM no longer contains, so the tooltip never visibly opens on a real tap. Registering the delegate with `{capture: true}` (fires document → ... → target, before the bubble phase reaches the `th`) and calling `e.stopPropagation()` once a real anchor is found fixes it, and has the added benefit of not sorting the column just because the user meant to read its tooltip. Any new tooltip-anchor nested inside another element with its own click handler needs this same treatment — a plain bubble-phase delegate only happens to work when the anchor has no clickable ancestor of its own (true for badges/dots, not true for header info-icons).
11. **Tooltip/hover-card body text must be 1-2 short sentences, not a paragraph.** `contractvalue.html`'s `METRIC_TIPS` originally ran 60-90 words each (written like code comments explaining *why* a formula works a certain way, not like end-user help text) — on a narrow card this wraps into 8-10 lines, which is both unreadable and, combined with guardrail below, gets clipped off-screen. Say what the number IS and, if truly needed, the one thing that makes it useful — cut the justification/rationale entirely, that belongs in a code comment near the calculation, not in the UI. **Separately:** a tooltip/hover-card that's always centered under its anchor (`left: 50%; transform: translateX(-50%)`) overflows off-screen once the anchor sits near the edge of a narrow container — confirmed by a user screenshot showing the leftmost stat's tooltip clipped on the left and the rightmost stat's clipped on the right, on the exact same card. `tokens.css`'s `.tooltip-anchor` component already has `.tooltip-align-left`/`.tooltip-align-right` modifiers for this; `contractvalue.html`'s bespoke `[data-tip]` mechanism got its own copy (`.tip-align-left`/`.tip-align-right`, same naming) applied to the first/last item in a row of stats. Any new multi-item row of tooltipped stats needs the edge items aligned this way from the start, not centered by default.
12. **`align-items: stretch` does NOT clamp a flex item back down to its container** — an item whose content-based minimum is wider than the container overflows it anyway, and (this is the part that makes it look like a whole-page bug rather than one bad element) *every sibling* then stretches to that same overflowed width, so the entire control bar, and with it the page, scrolls sideways. Found 2026-09-21 on `index.html`'s control bar: a long contract name in "Booking As" ("The Villas at Disney's Grand Californian Hotel & Spa") made all four control groups 412px wide inside a 353px container at 390px viewport. `min-width: 0` alone does NOT fix this — an explicit `max-width: 100%` on the stretched item is what actually caps it (`.control-group` in `styles.css`). Pair it with `flex: 1; min-width: 0` on the truncating text span itself, or the `text-overflow: ellipsis` never fires: a flex item's default `min-width: auto` refuses to shrink below its own min-content, which for `white-space: nowrap` text is the whole string. And any badge/pill that must survive the truncation has to be a SIBLING of that span (`flex-shrink: 0`), not nested inside it — nested, it's the first thing the ellipsis eats.

---

## Future Feature Ideas

Sourced from DVC community forums (DISboards, DVCNews, Reddit r/dvcmember). Roughly prioritized by community demand.

| # | Feature | Description | Status |
|---|---------|-------------|--------|
| 1 | Cross-resort date comparison | Pick dates, see point costs across all resorts side-by-side | Done |
| 2 | Optimal date finder | Given resort/room/# nights/date range, find the cheapest window | Not planned |
| 3 | Weekday/weekend optimization hints | Show "save X points by shifting check-in by 1 day" | Not planned |
| 4 | Split stay calculator | Model multi-segment stays across resorts to save points | Done |
| 5 | Banking/borrowing use year planner | Each contract shows its real next deadline in My Contracts. Contracts also track a manually-maintained points ledger, one row per use-year cycle (`contract_year_points` table, `db/migrations/007_add_contract_year_points_ledger.sql`, superseding migration 006's flat-columns approach) — edited as a small table in My Contracts (current + next year always shown, plus optional further-future rows via "+ Plan a year"), and the calendar's "Booking As" card shows what a selected stay would leave in the current year's balance. Use-year labels follow DVC's own convention (a cycle is labeled by its deposit year, not its expiration year — researched 2026-09-11). Reminder emails (Supabase Edge Functions + Resend, `supabase/functions/`) were deployed and verified live 2026-09-05 against `dvcalc.app`, with a daily `pg_cron` schedule — see `docs/phase5_deployment.md`. Since the cron invokes the function fire-and-forget (nothing reads its response), `send-banking-reminders` now also writes a heartbeat row to `reminder_run_log` on every run (`db/migrations/017_add_reminder_run_log.sql`), and the nightly digest (`scripts/nightly_watchdog.py`'s `check_banking_reminders()`) reads it to flag a stalled cron or a run full of errors — see `docs/nightly_pipeline_plan.md`'s "Tier 2b." The ledger has a 4th bucket, Holding (`points_holding`, `db/migrations/015_add_holding_points.sql`) — DVC's real "parking space" for points from a reservation modified/canceled 1-30 days before check-in, which can't be banked or borrowed, expire with their use year, and can only book stays no more than 60 days before check-in (researched 2026-09-19, confirmed against DVC Resale Market and Disney's own FAQ). `dvc-ledger.js` (loaded on `index.html`/`account.html`, same plain-`<script>`-exposing-`window.DVCLedger` convention as `dvc-dates.js`) holds this and the other DVC-rule pure functions — `pointsEnterHolding(daysBeforeCheckIn)` and `validateBorrowedPoints(borrowed, nextYearPointsPerYear)` (DVC's real borrow ceiling is 100% of next year's allotment, not the 50% this briefly, incorrectly shipped as on 2026-09-19 before being corrected the same day — a 50% cap was only ever a temporary COVID-era restriction, April-July 2020 — surfaced as an advisory note under the Borrowed stepper, not a blocking validation — this app can't check the real number against Disney's system). Covered by `tests/dvc-ledger.test.js` (`node --test`, no framework/dependency). The calendar's "Smart Draw" recommendation card (`app.js`, `computeSmartDraw()` — an auto point-allocation engine that already existed before Holding was added: draws from a contract's ledger in priority order, writes the result back via the same `upsertContractYearPoints()` the ledger steppers use, with manual override/undo) now draws Holding first, ahead of Banked/Borrowed/Current, since holding points are the least flexible of the four. Holding points expire at the end of their use year; the 60 days is an advance-booking window (a stay booked with Holding points must check in within 60 days of booking), not a rebook-within-60-days-of-cancellation clock -- an earlier version had this backwards, corrected against Disney's own On-Line Booking Terms & Conditions (effective 2025-06-01). `dvc-ledger.js`'s `holdingExpiration()` returns the use-year end, and `points_holding_entered_at` (migration 016) is still carried through saves but no longer drives any deadline. Three cancel/leftover-points aids sit on top of this (2026-09-22, from the r/dvcmember research in `docs/dvc_math_todo.md`, which also records every rule and its source): (1) Smart Draw's "If you cancel by {date}" line (`buildCancelOutcomeHTML()` in `app.js`, rules in `dvc-ledger.js`'s `cancellationOutcome()`) -- 31+ days out, points return to the stay's use year, but only CURRENT-year points come back bankable and only until that year's banking deadline (banked/borrowed moves are final); 1-30 days out is the existing Holding guardrail; (2) a bank-first suggestion (`shouldSuggestBankFirst()`) for a stay after its use year's banking deadline, worded as "not booked yet and not sure you'll go?" on purpose -- once booked, bank-then-borrow gives no cancellation protection, since canceled borrowed points also return to the stay's use year unbankable; (3) short-notice stay ideas on the ledger's "🔒 N pts expire" state (`dvc-leftover-points.js`'s `findStays()`, wired by `account.html`'s `buildLeftoverStaysHTML()`). Expiring points always mean booking close in, when most resorts are sold out, so a stay only qualifies when DVC Field Guide history (`data/availability_data.js`, at the booking window nearest that check-in's lead time) gives it Good+ odds -- same 0.75×nights bar as Booking Outlook -- and results rank Walt Disney World resorts first (per user direction -- off-site Vero Beach/Hilton Head/Aulani often have the best short-notice odds but are rarely a realistic last-minute trip for an Orlando-focused owner), then Excellent odds before Good, then most-reliably-available first -- within one resort, too, an Excellent-odds stay beats a longer Good-odds one, on the user's direction that likelihood of actually getting the room matters more than using every point (in practice Saratoga Springs, Old Key West, Boulder Ridge lead; AKV/BWV/BCV/Riviera/Cabins are ~0 at 1-3 months). A first version ranked purely by longest affordable stay and was rejected on user feedback for suggesting stays that are almost always sold out. It honors this contract's own 11/7-month booking windows (resale-restricted resorts excluded via `getUserResortAccess([contract])`) and Holding's 60-day rule, links each idea to the calendar via `DVCCompareHandoff.url()`, and its empty state distinguishes "too few points for any night" from "no resort you can book usually has openings this close in," both pointing to a member transfer. The date -> key-dates-period mapping is a copy of `app.js`'s `getKeyDatesPeriod()` -- keep them in step, and don't narrow it for the finder alone: it maps whole months onto the Field Guide's measured holiday weeks (all of November reads as Thanksgiving week), which likely undersells quiet weeks, but the finder deliberately stays consistent with Booking Outlook rather than second-guessing the source data (user decision, 2026-09-22). Results are cached per contract/balance since the ledger re-renders on every stepper edit. `home.html`'s dashboard leads with an At-Risk Points Health Banner (`home.js`'s `renderHealthBanner()`, its own copy of `evaluateContractDeadlineStatus()`/`computeEarliestDeadline()` since `home.js` duplicates rather than shares helpers with other pages) — colored by `dvc-dates.js`'s shared scale: a still-bankable state uses `urgencyTier()` (green over 60 days, yellow 31-60, red 30 or fewer), while a "use it or lose it" state (banking window closed) uses `expirationTier()`, which is the same scale but never green (2026-09-22 — a green banner reading "or they're forfeited" was flagged as a contradiction in a new-owner UX review). The same `expirationTier()` colors My Contracts' use-by countdown, Notification Center items and ledger line. A use-by state's action row omits "Bank Now". Below the banner, the My Contracts widget shows **Available Now** (sum of every active contract's confirmed current-cycle balance, with a "+ N not added" note for unconfirmed ones) and a **Coming up** list — `DVCPointAttention.timeline()`, every contract's current-cycle bank-by/use-by/add-balance action in date order — so a second contract's deadline isn't hidden behind the banner's single earliest one. Tapping an expandable banner reveals "Bank Now →" (`account.html`) and/or "Draft a Trip →" (`suggest.html`) links. `home.html` doesn't load `tokens.css` (see the "Only index.html and account.html consume this file" note above), so the banner's green/yellow/red hex values are hand-matched to `--color-good`/the warning color account.html's own `.deadline-countdown.warning` actually applies/`--color-danger` rather than shared via a stylesheet | Done |
| 6 | Booking window indicator | Mark 11-month and 7-month booking window open dates on calendar | Done |
| 7 | Points budget reverse calculator | "I have X points — show me what I can book" — `suggest.html`, "Suggest a Stay," searches every resort/room for the longest stay a given budget affords in a target month, optionally scoped to resorts a signed-in user's contracts can actually book | Done |
| 8 | Year-over-year point changes | Highlight point increases/decreases between years | Done |
| 8a | Multi-resort YoY comparison | Pin up to 4 resort/room combos on the YoY chart to compare trends side-by-side | Done |
| 9 | True cost per night (ownership) | Factor in amortized purchase price + dues over remaining contract | Done |
| 10 | Cash value score | "Dollars saved per point" metric for each booking | Planned |
| 11 | Owner vs. rental / owner vs. cash savings tiles | Cost Comparison card used to show separate "Owner vs. cash" and "Owner vs. rental" savings tiles; removed 2026-08-28 to compact the card down to 3 tiles (Disney / Owner / Rental). Rental tile still shows rental-vs-cash savings inline. Could re-add owner-side savings as an optional expandable section. | Removed, could re-add |
| 12 | Saved trips / itineraries | Save a planned stay (single or split) from the calendar and reload it later | Done |
| 13 | Itinerary comparison | Compare up to 3 saved itineraries side by side: points, nights, cash value, $/point, crowd level | Done |
| 14 | Contract Value ("which resort should I buy") | Ranks all 17 resorts by long-term vacation value per dollar spent, given points/purchase type/years/growth-rate assumptions -- real resale &amp; direct pricing (`data/resort_investment.js`) and contract expiration dates, dues growth defaulting to each resort's own historical trend | Done |
| 15 | Hotel construction/refurbishment alerts | "Resort Alerts" card in the summary panel (`buildResortAlertsHTML()` in `app.js`, data in `data/resort_construction.js`) flags ongoing/upcoming construction for the selected resort, calling out entries that overlap the selected stay dates. Sourced from Disney Food Blog's DFB Disney World Calendar PDF, transcribed 2026-09-13 | Done |
| 16 | Special events overlay (festivals, hard-ticket parties, runDisney weekends) | 🎉 badge (bottom-right corner) on calendar days with a date-specific ticketed party/race weekend, with a hover tooltip; broader festival/seasonal windows surface once in the "Special Events & Festivals" summary card instead (`getEventsForDate()` in `data/disney_events.js`). Sourced from the same DFB PDF as #15; only events with an explicit date range in the source were included | Done |
| 17 | High Crowds / Holiday Weekend flags | DFB's PDF also flags "High Crowds"/"Holiday Weekend" days inline on its calendar grid, separate from the Undercover Tourist crowd scores DVC Companion already shows (#6 area). Skipped for now since it'd likely be redundant with the existing 1-10 crowd calendar | Not planned |
| 18 | "What's New" / permanently-closed park news | DFB's PDF has a "What Opened in [Year]" / "Permanently Closed in [Year]" editorial section (new attractions, lands, restaurants, store closures) by park/hotel. Skipped as out of scope for a points calculator — it's not date/booking-decision-relevant, and DFB's own guide would stay more current than a copy we'd have to keep re-syncing | Not planned |
| 19 | Free vs. paid subscriber tiers | Active Member ($49.99/yr, 7-day trial): `subscriptions` table (migration 014), Stripe checkout/portal/webhook Edge Functions, and a client-side owner-data gate in `auth.js` -- all built against Stripe test mode. The gate ships off (`MEMBERSHIP_GATE_ENABLED = false`) until live-mode Stripe exists. The "Active Member Pass" Trophy Room badge that would key off it is still unbuilt (Phase 7). See `docs/subscriptions_plan.md` | Built, not live |

---

## Google Play Store Submission (Deferred until MVP)

Investigated 2026-09-20. PWA installability (`manifest.json`, `service-worker.js`, icons) is already in place from the earlier PWA work, and `.well-known/assetlinks.json` exists as a template with two placeholders (`package_name`, `sha256_cert_fingerprints`) to fill in when this is picked back up. `privacy.html` was checked against `db/schema.sql`/`dvc-track.js`/the GoatCounter snippet and is accurate as-is. Remaining steps, not started:

1. Confirm the static host serves `.well-known/` paths (some exclude dot-directories by default).
2. Run PWABuilder (pwabuilder.com) against the live site to generate the signed `.aab` and a real Play signing key — gives the real `package_name` and `sha256_cert_fingerprints` for `assetlinks.json`.
3. Create a Google Play Console developer account ($25 one-time).
4. Fill out the Data Safety form in Play Console from `privacy.html`'s disclosures (email/name via Google sign-in, contract/trip/itinerary data, push subscription, Stripe subscription status).
5. Store listing assets: feature graphic (1024×500), phone screenshots (2+ min), short/full description, content rating questionnaire.
6. Submit to Internal Testing first and confirm the TWA launches full-screen with no URL bar (confirms `assetlinks.json` is correct) before promoting to Closed/Production.
